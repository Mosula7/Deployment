from setuptools import setup

setup(name='churn_utils',
      version='0.1',
      description='functions for churn project',
      author='Mosula',
      packages=['churn_utils'],
      zip_safe=False)
